// Función para iniciar sesión
function iniciarSesion() {
    const username = document.getElementById('username').value;
    const password = document.getElementById('password').value;
    // Aquí puedes agregar la lógica para la autenticación de usuario
    alert('Inicio de sesión no implementado.');
}

// Función para ver propiedades
function verPropiedades() {
    // Aquí puedes agregar la lógica para redirigir a la página de propiedades
    alert('Función para ver todas las propiedades no implementada.');
}